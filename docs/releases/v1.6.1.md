# 绘遇 HUIYU 1.6.1

修复 1.6.0 安装后只有任务栏图标、画室与桌宠无法启动的问题。

- 安装包补齐网关必需的文档重定向文件及文档目录；此前缺失导致网关反复退出，桌宠一直等待。
- 新快捷方式显式使用绘遇图标，安装后通知 Windows 刷新图标缓存。
- 首次网关启动失败显示原因和日志位置；后台恢复后补建桌宠窗口，启动超时清理本次子进程。
- 部署工具识别实际安装目录，支持对 1.6.0 进行缺失资源及图标的定向修复。
- 打包前新增隔离安装目录启动验证，使用内置 Node 检查网关、画室、桌宠、聊天和文档，避免开发环境掩盖缺包。

样张仍从安装目录旁的 AI/SceneShowcase 或用户配置工作区读取。本次故障机器的原有样张文件完好，网关恢复后已验证原图、缩略图和首页预览可访问。

本版沿用手动安装发布方式。自动更新仍等待持有原签名私钥的主机签名，不更换已有更新公钥。
