# 非主机代码修复与验证 · 2026-09-08

本次按用户约束仅修改代码和文档，不生成真实样张、不下载模型、不修改提示词与分级数据，不部署桌面主机。

## 修复

- 复用 monolith --update-baseline 移除 3 个已低于 600 行的历史豁免，存量预算只降不升（20 → 17），未放宽上限。
- useFocusTrap 的导入改为带扩展名的相对路径，Node 原生 TypeScript 测试可解析；不改变确认弹窗或焦点管理行为。
- 对比度检查从单一深色扩展到双主题全局文字、状态图标及全部已注册角色强调色。精确读取主题 CSS，支持嵌套 var/color-mix、透明度与十六进制 alpha；必查令牌无法解析时失败，避免跳过后误报通过。
- 角色强调字在深色上适当提亮、浅色上加深；原始角色色继续控制氛围光，不修改人物图像。静态 SFC 扫描仍是启发式深色检查，不能宣称所有组件已经达到双主题 AA。
- 参考图检查与网关复用 resolveCharRefRoot，统一显式目录、AI 工作区及本地旧目录；修复旧路径漏层、忽略显式配置、目录被当成图片、非 pending 缺 URL 被跳过等问题。
- 显式素材目录无效时不切到另一套素材；缺素材仍失败并提示先检查目录/同步。校验器不再提示直接改写索引来解决缺图。
- 修复浏览器巡检对 color(srgb ...) 背景的解析遗漏：此前把 color-mix 深色遮罩当透明，误报角色页白字压白底。角色页两主题复测通过，未改动其图片或遮罩。
- 主题浏览器巡检恢复浅色，并补充每个注册角色的真实计算样式 AA 检查。全部测试使用本地页面或构造文件夹，无真实生成调用。

## 验证

- 类型检查、ESLint、样式、动画、体量与生产构建通过；18 路由预算通过。
- 前端测试 264 项；Node 脚本单测 398 项；后端契约 25 组通过。
- 新增参考路径与非法 URL 回归、颜色解析回归通过；双主题角色强调色 888 项静态组合通过。
- 工作台与控制台双主题、浏览器角色强调色 6 项定向回归通过，已查看两套工作台截图。
- 最终全站深浅主题巡检 48 项全部通过（覆盖页面错误、横向溢出、严重不可读文本及角色强调色 AA）；这不是对图片叠字及所有动态组件的全量 AA 认证。原始日志位于 runtime/code-repair-*.log，截图为 runtime/theme-workspace-dark.png / theme-workspace-light.png（本机资料，不入库）。

## 未消除的门禁失败

npm run check 仍有 5 项失败：scenes:optimize、scenes:ratings、scenes:validate 涉及已有内容；content-contracts、ref-urls 因本机未配置参考图目录、缺少 2534 个真实文件。245 条 pending 单独计数。

这些失败不通过修改提示词、降低门禁或伪造素材消除。主机需核对素材路径并同步参考图；涉及画面语义的场景改动必须完成真实出图后再交付。
