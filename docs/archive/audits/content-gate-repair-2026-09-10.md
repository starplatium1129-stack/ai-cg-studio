# 内容门禁修复与回归记录

> 源码基线：`962c9fc261e4cb3b993c20749d450046bdff5b53`。执行环境：Node v24.18.0，GitHub Actions Linux；时间按 UTC 记录。此报告只收口已确认的内容门禁失败，不表示 467 条剩余场景语义审计完成。

## 根因与修复

原 node unit 套件 454 项：15 项失败。其中 12 项被同一个缺少安全默认服装蓝图的问题连带阻塞；另外涉及服装环境词、服装覆盖、句号数量判断。新定向断言在修复前真实失败 5 项，修复后全部通过。

1. **服装环境词归属。** 将 krista_lenz / coronation_winter_wall 的 winter、murasame / festival_red_yukata_no_fan 的 festival 移到唯一引用它们的全年龄场景，不放宽污染扫描器，不删除季节与祭典意图。
2. **完整枚举 11 套未被引用的服装。** 为它们分别编写 11 条普通日常／演出场景；保留原有服装 ID、默认选择和 1,681 条存量蓝图。海梦的默认校服终于有精确匹配的全年龄蓝图，原量身场景仍保留已修好的水手服，不靠任意换装或成人回退修复默认样张。各 ID 见[固定覆盖清单](../../../scripts/tests/fixtures/scene-coverage-repairs.json)。
3. **13 条完整单句被句号数误判。** 这些原文已经含动作和具体日光、雨雾、台灯等环境描述。本次保持全部存量 prose 不变；结构门禁允许完整、足够实质且自带具体环境的单句，并新增质量词堆砌、短句、截断句等反例。它不是自动语义或画面审核器，其余词条、长度、角色、分级与双引擎约束仍保留。
4. **团扇词型。** 保留丛雨的旧浴衣形态和团扇，去除与 round paper fan / uchiwa 矛盾的 folding_fan；捞金鱼场景继续绑定无扇变体，不再强占握纸网的手。

**同一旧测试中被遮蔽的景深检查也已复验。** 艾莲的茶点服务场景已在英文正文与两个引擎载荷中保留 depth of field，原测试只读 token 数组而误报；改为在正文承担该要素时进一步断言两个实际载荷保留它，没有给场景重复追加标签。首轮完整门禁的失败记录保留在机器证据。

## 实际验证

- `npm run wf -- gate:full --all --verbose`：通过，使用项目原有结构素材模式；包含 check、前端、node unit、接口契约与构建预算。
- `npx vitest run --coverage`：通过；具体文件数／用例数保存在机器证据的 coverage 摘要，不以旧结果冒充。
- 独立 node unit：466 项，465 通过、0 失败、1 跳过；保留既有跳过，不把它算成通过。
- `npm run test:e2e:critical:run`：通过；是模拟服务的浏览器关键回归，不是真实模型。
- 新增定向回归 12 项、定稿保护 100 条、内容契约、11 条新增内容完整性检查均通过。
- 2048 份存量 SFW 双引擎计划重新比较；变化只允许落在两处环境词迁移的目标场景。新增内容保留 22 份编译摘要。
- 657 条成人蓝图、全部独立场景源文件和定稿基线不变；没有修改年龄资格、生成限制、参考索引或发布 manifest。

命令、退出码、源文件 SHA256、编译摘要与[实际运行](https://github.com/starplatium1129-stack/huiyu/actions/runs/34502060844)见[机器证据](../../evidence/content-gate-repair-2026-09-10.json)。

## 尚未验收

**本轮没有 GPU 出图、打开图片或发布样张。** 新增 11 条场景与两个词条迁移的真实模型效果仍须主力机验收；人工看图继续由用户后置，所有新条目的 renderStatus 都保留 pending-local-render。默认素材模式仍需要真实素材根目录，不能用空目录或占位图片造绿。

这些是修复现有门禁的派生创作场景，并非声称出自官方剧情；没有把参考 URL 存在、编译成功或测试通过写成视觉通过。原 467 条审计队列未被关闭，新角色对账、Windows 原生、真实语音与完整素材验收继续见[未来规划](../../roadmap.md)。
