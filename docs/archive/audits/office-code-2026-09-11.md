# 办公机代码收尾与验证（2026-09-11）

> 历史审计快照（2026-09-12 整理）：正文保留当时证据，旧结论和下一步不代表当前状态或授权。当前事实见 [办公机独立审计](office-independent-audit-2026-09-12.md)，尚未完成及暂停事项见 [未来规划](../../roadmap.md)。归档不表示其中问题全部解决。

基线：`010a5f5`。范围为本机代码、浏览器和真实子进程可验证的待办，不恢复暂停中的场景审计。命令、结果、构建与浏览器性能对照见[机器证据](../../evidence/office-code-2026-09-11.json)。

最终验收位于独立 worktree `D:/code/ai-cg-studio-office-check-20260911`，只复制本任务文件与当前聚合数据，依赖目录复用本机已安装内容。其他会话的独立审查报告及其索引改动保留在原工作区，未纳入本次交付。以下完整门禁结论针对这个受控副本，不代表包含其他会话未提交内容的共享工作区已通过。

## 交付内容

### 桌面工具取消

生产桌面桥将 AbortSignal 传给网关请求；HTTP 断开触发网关取消，命令与截图子进程受同一生命周期管理。取消、超时、输出超过上限会终止进程树并等待退出；无法确认退出时返回明确错误。非零退出即使带有输出也不再报告成功。写文件在提交前再次检查取消，并清理临时文件。

测试直接执行 Rust 文件内的生产桥接 JavaScript，连到隔离的真实 HTTP 网关，启动父进程和子进程后取消，核验二者退出及其他请求不受影响。现有本机授权、参数白名单和成年内容边界保持原约束。这不等于已完成安装后的 WebView2、麦克风和视觉模型验收，也不承诺撤销取消前已完成的文件或外部操作。

### 绘图加载与首次入册

换装和批量面板第一次使用时才加载；使用后保留实例，批量选项和运行任务不因关闭弹窗而销毁。换装仍沿用关闭时释放上传图和遮罩、重新打开时重置角色选择的既有规则。热门角色选择器按来源加载，画面对比独立按需加载，保留焦点锁定、Escape 关闭与焦点返回。

入册、恢复与空态统一使用“存入作品册”。正在保存时阻止重复提交，失败允许重试，成功后给出作品册入口。保存开始即冻结画面、seed、提示词及归属字段；读取图片或保存期间换图，不会把后来的画面标为已入册，也不会清除它的临时缓冲。

所有原硬预算保持不变。绘图路由从 138.6 降至 138.3 KiB，CSS 从 111.5 降至 109.5 KiB；诊断元数据增加共享代码后，入口闭包从 359.6 增至 361.8 KiB，绘图静态闭包从 538.1 增至 540.0 KiB。仍有 4 项超过预算的 90% 提醒，不能宣称所有包都缩小或预算余量充裕。冷／热进入及首次操作使用每阶段 3 个浏览器样本，只作本机回归对照，不代表真实模型生成耗时。

最终独立测量使用 Edge 152.0.4191.66、单 worker、1440 × 960，本地产物，无其他浏览器测试并行。冷进入 591／534／565 ms，首次操作 321／279／274 ms；同上下文热进入 453／402／389 ms，首次操作 252／257／362 ms。早期基线来自共享工作区，机器负载及缓存条件并不严格一致；原始数据保留，但不据此给出整体加速百分比或声称所有操作都更快。

### 主动导出的脱敏诊断

诊断包采用字段白名单，保存应用／数据版本、有限运行状态、最多 200 条当前页面会话内的客户端调用和任务状态。任务 ID 转为本地别名，请求只保留固定路由类别、方法、耗时、可取得的失败状态码及成功／失败／取消结果。明确标记活跃任务是同时发生的上下文，不能把并发关联当作确定的请求归属。

不保存或导出请求体、请求头、任意错误正文、聊天、图片、提示词、完整配置、用户目录、密钥或 token 后缀；不新增遥测上传。记录范围是共享 API 客户端和任务中心，页面重载后重新计数，不能代替全量服务端追踪。导出按钮阻止并发点击，下载失败可重试。原服务端诊断脱敏同时补齐 API key、嵌套数组、带空格的引号凭据和认证 URL。

### 桌宠日程与事件演出

复用现有时间片问候、闲置提醒和服务／入册事件，以数据表绑定已支持的表情、好感度门槛及过期时间。台词使用现有角色内容，不生成新正史，不调用 LLM，不写入聊天历史，不执行工具，也不直接修改作者 motion 参数。

演出复用房间现有语音与 Live2D 生命周期。聊天、输入、语音收听、准备服务、勿扰、安静时段和隐藏窗口优先；更换角色、卸载或超时取消自己持有的播报并释放音频缓存，不停止其他聊天音频。播报服从实时配音开关，声线未就绪时只显示已有气泡／表情；陈旧事件不补播。同一提醒只消费一次，队列、已播放标识和最长合成等待均有界。

### 路由与文档状态

新浏览器上下文验证普通页面→聊天→桌宠→普通页面，以及前进／后退、`/chat/`、带查询参数路径和 sessionStorage 被禁用。核验实际文档变化与响应的脚本安全策略，普通页面不获得 `unsafe-eval`。

夏目旧叠层研究已标注后续实现与验收入口；早期未勾选提案和“统一写 0”假设不再冒充当前待办。本次没有据旧报告宣称重新完成原生视觉验收。

### 依赖与测试隔离

按 ONNX 已有 `^0.6.0` 兼容范围将 `adm-zip` 从 0.6.0 升至 0.6.1，只变化一个依赖及锁文件中的对应记录；没有降级 ONNX、执行安装生命周期脚本或下载模型。完整与生产依赖审计本轮均返回零项告警。同步、异步及单条解压的三个回归均验证正常提取，并确认预先存在的目录链接被拒绝、外部哨兵文件保持不变；所在安全套件 17 项全部通过。

[上游漏洞公告](https://github.com/advisories/GHSA-vwc7-r8mq-g2x9)在本轮读取时仍显示修复版本为 None；本次升级以 npm registry 发布内容、包差异和本地回归为依据，不声称公告已经同步更新。审计零告警也不能替代主力机完整安装、实际推理或整个软件的安全审查。

共用模拟服务的套件归入单 worker 项目；统一端口映射支持隔离运行，相关布局测试改为跟随当前项目地址。浏览器回归与性能测量分开执行。性能配置使用单一配置对象，避免合并项目列表时意外带入 463 个回归用例；最终列表与执行均只含一个基准。

## 验证与边界

- 完整质量门禁使用 `AICS_REFERENCE_AUDIT_MODE=structure`，最终通过，耗时 2 分 4 秒，包含质量检查、前端单测、466 用例 unit 套件、25 组接口契约和生产构建。结构模式不能替代参考图片文件核验。后续只调整测试配置、浏览器用例和文档，再以类型检查、对应浏览器回归、工作流和文档检查复核。
- 最终将关键回归与受影响的专家布局、控制面板、首页导航用例合并运行，319 项全部通过，0 失败、0 跳过、0 重试后通过，耗时约 3.9 分钟。此前 301 项关键回归、24 项补充回归及 2 项图片可见性定向复验也通过；它们存在重叠，以最终 319 项作为交付口径，不累加覆盖数。独立性能基准 1 项通过。
- 深浅主题、390 像素窄屏键盘旅程通过。入册按钮对比度为 10.71／7.93，对比弹窗标题前导文字为 7.21／6.37，诊断导出按钮为 9.26／8.72，均不低于 4.5。对比度在入场动画完成后测量，未放宽阈值或忽略图片背景异常。
- 视觉复核使用明确标注为 UI TEST IMAGE 的 832 × 1216 棋盘格图片：真实模拟网关的结果请求仍须返回成功和图片类型，再用测试画幅检验布局、缩放、焦点和作品册展示。作品册还必须实际加载图片并完成入场显现，不能只凭条目数量通过。它替代早期 1 像素占位图，不能当作真实 CG 或模型生成质量证据。
- 中间失败如实保留：共享模拟服务发生 reset 干扰，共享 dist 构建期间出现页面缺失；移入隔离端口后发现部分旧布局测试仍写死 3000。类型声明已补齐，独立 worktree 首次缺少 dist 的聊天契约在初始化构建后通过。共享工作区另一会话报告的尾部空格未擅自修改。补充首页用例还修正了旧入口文案和对临时视觉选角的持久化假设，同时保留实际进入所选角色房间的断言。
- 未进行真实 GPU 出图／出片、参考图片补齐、安装包构建／部署或已安装程序的音画质量验收。467 条语义队列保持暂停，290 张候选仍待人工审核，pinned 字节基线未改动。6 份角色喜好需要可靠来源，视频剧本扩写属于内容与成片验收任务，均未冒充代码修复完成；新模型选型维持待决策。

验证副本中的日志位于 `runtime/office-code-2026-09-11/`，截图位于 `.review-shots/office-code-*`。交付时另复制到原工作区 `runtime/office-code-2026-09-11/verified/`，其中 `shots/` 保存对应截图；早期失败 trace 留在各自运行目录。没有将模拟输出当作真实作品交付。
